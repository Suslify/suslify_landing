This folder contains generated placeholder “screens” so the landing page looks complete.

    Currently used by the landing page : -home.svg -
    cart.svg - item.svg

                   If you later export real screenshots,
    you can replace these files(keep the same filenames),
    or update the paths in suslify_landing / src / App.jsx.
